# Task 025 Path Law Discovery

This report directory contains the deterministic arithmetic path-law discovery
campaign for the Schwarzschild four-form battery.

## Artifacts

- `campaign_output.json` is generated by:

```bash
PYTHONPATH=src python -m lloyd_v4.evals.path_law_campaign
```

The campaign fits the fixed 17-term candidate library with sparse OLS fits of
size 1, 2, and 3. The emitted `PathEnvelopeLawDiscovery` values are report
objects only; no V4 primitive, status family, manifest entry, or transition rule
was added.

## Headline Results

| Form | Status | Top 1-term fit | Top selected fit | Axes passed |
| --- | --- | --- | --- | --- |
| F1 | `path_law_supported_envelope` | `T_16`, c=-0.43464231440804224 | `T_13 + T_15`, R2=0.2621103091007638 | A, B, E |
| F2 | `path_law_supported_envelope` | `T_7`, c=-5.8982831699991455e-18 | `T_1 + T_2 + T_3`, R2=0.29495978731107164 | A |
| F3 | `path_law_exact_zero` | none | zero law | A, B, C, D, E, F |
| F4 | `path_law_supported_closed_form` | `T_15`, c=1.0111298989021043 | `T_15`, R2=0.9578834217812657 | A, B, C, D, E, F |

## F4 Rediscovery Gate

Pass. The top-ranked 1-term fit for F4 selected `T_15`, the
`delta_f/(2*sqrt(f))` candidate, with coefficient `1.0111298989021043`
and R2 `0.9578834217812657`. The precision-scaling axis also passed:
the median predicted float32 / observed float32 ratio was
`0.9948202827321421`.

## Honest Limits

- F1 and F2 did not produce closed-form sparse laws at the declared R2 limit.
  Their chain-envelope bound held with no exceedances, so they were classified
  as envelope-supported.
- F2 failed precision scaling for its selected smooth fit; the median predicted
  / observed float32 ratio was `1.1724403427279958e-09`.
- F4 is a rediscovered closed-form response law, not an envelope predictor.
  Promotion to substrate observable remains downstream.
